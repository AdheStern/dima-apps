name: dima-prod

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: dima-app-prod
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
